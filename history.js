// history.back returns the previous page
history.back();
// history.back returns the next page
history.forward();
// history.go returns back and forward page . -(back), +(forward)
history.go(-1);
// history.length count the total numbers of history
history.length();
