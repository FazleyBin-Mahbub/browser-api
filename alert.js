// alert something
alert("this is an example of alert");

// confirm something with to ok and cancel button. if user click cancel it's returns false and user click ok it's return true , on this case we can do any operation using condition or something
confirm("raji?");

const userInput = () => {
  // use prompt for getting user input
  prompt("What is your name?");
};
